# LeetSwipe API configuration
# MongoDB Atlas connection string (same cluster the generator writes to).
MONGODB_KEY=
# Secret used to sign anonymous JWTs. Set a long random value in production.
JWT_SECRET=
# Port the API listens on (default 4000).
PORT=4000
